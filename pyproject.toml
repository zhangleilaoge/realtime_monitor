[project]
name = "realtime-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "psutil>=7.1.3",
    "pyadl>=0.1",
]
